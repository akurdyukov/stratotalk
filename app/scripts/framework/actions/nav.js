// @flow

import { createActions } from 'redux-actions';

import { ActionTypes } from '../constants';

export default createActions({
  [ActionTypes.GO_PRIVATE]: undefined,
  [ActionTypes.GO_SCENARIOS]: undefined,
});
